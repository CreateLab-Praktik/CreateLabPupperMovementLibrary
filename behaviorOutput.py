import BehaviorLibrary as Behavior
import time

def behaviorPool():

    ## Message should be objects instead of functions

    ## Raw message Loop

    while True:

        if any non-rawMessages:
            duration = non-rawMessage.duration
            flag = 1
            while flag = 1
                send(non-rawMessage)
                time.sleep(1)
                duration --1
                if duration = 0
                    flag = 0
        
                


        send(newRawMessage())
        

   